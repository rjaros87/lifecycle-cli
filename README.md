# lifecycle-cli
Native CLI (Micronaut + Picocli + GraalVM) for Kubernetes lifecycle hooks — preStop, health, and shutdown — against a Micronaut management endpoint. Built to run inside distroless images with no shell.
